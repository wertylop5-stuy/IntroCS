;  hw#27
;  boolean operators
;   "Stanley Lin <slin55055@gmail.com> period9
;   worked with no one
;   help from no one
;   time started: 10 min

(display "#2 pictures to code") (newline)
(define a
  (lambda (num)
    (or (and (< -1 num) (<= num 3))
        (and (<= 4 num) (< num 6)))))

(display (a -1))(newline)
(display (a 3))(newline)
(display (a 6))(newline)

(display "-----------------") (newline) (newline)

(define a
  (lambda (num)
    